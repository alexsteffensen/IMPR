#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int main(void) {

  int between, n, k, divisor, digit, num, lowercase, ch;

  if(n < -k || n > +k)
    between = 0;
  else
    between = 1;
  
  if(num % divisor == 0)
    divisor = 1;
  else
    divisor = 0;  

  if(ch == toupper(ch))
    lowercase = 1;
  else
    lowercase = 0;

  


}